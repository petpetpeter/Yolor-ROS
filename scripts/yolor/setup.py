from setuptools import setup

setup(
    name="yolor",
    version='0.0.1',
    install_requires=[
                      'numpy',
                      'matplotlib',
                      "rospkg",
                      "scipy",
                      ]
)